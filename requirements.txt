# Instale SIEMPRE las dependencias con el Python de la .venv:
# .\.venv\Scripts\python.exe -m pip install -r requirements.txt
#
# PyTorch y Torchvision NO se fijan aquí porque este proyecto requiere una build
# CUDA para entrenamiento con GPU y la etiqueta exacta (+cuXXX) depende de la
# rueda instalada en el entorno. Ver docs/guia_ejecucion.md para el orden correcto.

numpy==2.1.1
pandas==2.2.3
matplotlib==3.10.1
seaborn==0.13.2
scikit-learn==1.6.1
scipy==1.15.2
Pillow==11.1.0
opencv-python-headless==4.11.0.86
PyYAML==6.0.2
tqdm==4.67.1
python-dotenv==1.1.0
jupyterlab==4.4.0
notebook==7.4.0
ipykernel==6.29.5
ipywidgets==8.1.6
mlflow==2.21.3
streamlit==1.44.1
transformers==4.50.3
accelerate==1.6.0
datasets==3.5.0
evaluate==0.4.3
ultralytics==8.3.102
albumentations==2.0.5
# COCO tools:
# En Windows este paquete suele ser la fuente de más fricción por wheels y ABI.
# Se deja fuera del bootstrap principal para no bloquear la creación de la .venv.
# Si más adelante hace falta para evaluación COCO estricta, se instala aparte.
